  Write an SQL query to show the top n (say 10) records of a table.
  select * from tablename order by salary DESC LIMIT 10

Write an SQL query to determine the nth (say n=5) highest salary from a table.
SELECT TOP1 salary from (select distinct top n salary from worker order by salary desc)order by salary asc
